name: use_gcp

on:
  workflow_dispatch:

jobs:

  connect:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - uses: ./.github/workflows/connect-gcp.yml

      # Runs a single command using the runners shell
      - name: Run buckets
        run: gsutil ls
